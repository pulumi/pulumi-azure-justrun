name: yaml_ex
runtime: yaml
description: A minimal Azure Native Pulumi YAML program

resources:
  # Create an Azure Resource Group
  webapp:
    type: azwebex:index:webapp
    properties:
      filePath: ./www

outputs:
  # Export the primary key of the Storage Account
  url: ${webapp.url}
